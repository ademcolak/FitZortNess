{
  "name": "fitzortness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Local Telegram MVP for rule-based fitness coaching with optional OpenAI responses.",
  "scripts": {
    "bot": "node src/start.js",
    "setup:check": "node src/checkSetup.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "ajv": "^8.20.0"
  }
}
